function soma(n1=0, n2=0){
return n1 + n2
}
 
console.log(soma(2))

// parametro opcional (n1=0) significa que se o n1 não for declarado, ele assume o valor de 0 e a soma ocorre normalmente. se vc colocar apenas n1, n2 e nao estabelecer 2 valores, o javascript retorna como NaN.
//

